{
  "name": "kd-hcblm-face2face",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "KOMPETENCES FACE2FACE — front dédié du département présentiel (K-SPEM). Espace participant (fiche d'ancrage, journal de bord, missions, certificat) + console formateur (cohortes, émargement, certification). Servi sur face2face.declick.digital.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4176 --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
