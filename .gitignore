node_modules/
dist/
*.log
.DS_Store
.env
.env.*
!.env.example
# Per-client deployment presets (real values). Keep the *.example templates.
deploy/presets/*.env
# Local backup artifacts (DB dumps, media archives, env copies).
deploy/backups/
