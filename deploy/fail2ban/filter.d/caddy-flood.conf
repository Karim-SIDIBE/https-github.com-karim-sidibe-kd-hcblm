# Filtre fail2ban — rafale de requêtes, quel que soit le statut : le motif de
# l'aspiration de site (des centaines de GET séquentiels en quelques secondes).
# Le seuil est réglé dans la jail (maxretry/findtime), pas ici.
[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"status":
ignoreregex =
