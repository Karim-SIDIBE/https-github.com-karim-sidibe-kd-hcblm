{
  "name": "kd-hcblm",
  "version": "0.1.0",
  "private": true,
  "description": "Kompetences Declick KD-HCBLM v2.0 — monorepo (shared contract + backend + learner PWA).",
  "workspaces": [
    "packages/*",
    "server",
    "web"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
