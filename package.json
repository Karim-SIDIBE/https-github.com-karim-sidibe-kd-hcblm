{
  "name": "kd-hcblm",
  "version": "0.1.0",
  "private": true,
  "description": "KOMPETENCES DECLICK K-HCBLM v2.2 — monorepo (shared contract + backend + learner PWA).",
  "workspaces": [
    "packages/*",
    "server",
    "web",
    "mobile",
    "admin",
    "entreprise"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.0"
  }
}
